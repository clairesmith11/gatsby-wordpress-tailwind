import React from 'react';
import { graphql } from "gatsby";

const post = () => {
    return (
        <div>
            <h1 className="text-center text-5xl font-semibold my-5 text-red-600">Hello world!</h1>
        </div>
    );
};

export default post;
